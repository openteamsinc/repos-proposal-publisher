{
  "$schema": "https://json.schemastore.org/package",
  "name": "repos-proposals",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "ncc build index.js -o dist && cp requirements.txt dist/ && cp main.py dist/"
  },
  "dependencies": {
    "@actions/core": "^1.6.0",
    "@actions/exec": "^1.1.0"
  },
  "files": [
    "index.js",
    "requirements.txt",
    "main.py"
  ]
}